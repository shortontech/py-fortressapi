from fortress_api.api_model import ApiModel


class GetCryptoPriceResponse(ApiModel):
    # The PropertyDefinition decorator/attribute is represented here as a comment for clarity.
    # In an actual Django/Python environment, you might want to use Python decorators or other methods
    # to handle such metadata.
    #
    # @PropertyDefinition(required=True, in='body', instanceOf='CryptoPriceObjectResponse')
    # price = ApiModel.fields.decimal(required=False)
    pass
